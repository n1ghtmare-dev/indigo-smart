# indigosmart.ru — правила обхода.
#
# ВАЖНО: клиентские лендинги (/landing/, /landing2/, /landing3/, /landing4/)
# намеренно НЕ закрыты здесь через Disallow. Запрет обхода помешал бы роботу
# прочитать <meta name="robots" content="noindex, nofollow"> на самих страницах,
# и они могли бы остаться в выдаче по внешним ссылкам — без сниппета, но остаться.
# Закрытие от индексации живёт в разметке каждой страницы, а не здесь.
#
# Сюда добавляются только пути, которые роботу нечего обходить в принципе.

User-agent: *
Disallow: /api/
